CREATE TABLE projekti(
	nimi varchar(40) NOT NULL,
	kuvaus varchar(80),
	projekti_id serial PRIMARY KEY,
	asiakas_id serial REFERENCES asiakas(asiakas_id)
);

CREATE TABLE asiakas(
	asiakas_id serial PRIMARY KEY,
	nimi varchar (40) NOT NULL
);

CREATE TABLE tyosyote(
	syote_id serial PRIMARY KEY,
	kuvaus varchar(80),
	paiva date NOT NULL,
	kesto decimal(2,1) NOT NULL,
	projekti_id serial REFERENCES projekti(projekti_id)
);



-- CREATE TABLE osallistuja(
-- 	henkilo_id serial,
-- 	projekti_id serial,
-- 	PRIMARY KEY (henkilo_id, projekti_id),
-- 	CONSTRAINT henkilo_henkilo_id_fkey FOREIGN KEY (henkilo_id)
-- 		REFERENCES henkilo(henkilo_id) MATCH SIMPLE
--     	ON UPDATE NO ACTION ON DELETE NO ACTION,
--   	CONSTRAINT projekti_projekti_id_fkey FOREIGN KEY (projekti_id)
--     	REFERENCES projekti (projekti_id) MATCH SIMPLE
--     	ON UPDATE NO ACTION ON DELETE NO ACTION
-- );